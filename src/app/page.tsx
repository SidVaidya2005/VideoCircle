// Placeholder Home. Feature 05 builds the real landing surface; this exists so the
// scaffold has something to render, build, and lint against.
export default function HomePage() {
  return (
    <main>
      <h1>VideoCircle</h1>
      <p>Start a meeting, share the link, talk.</p>
    </main>
  );
}
