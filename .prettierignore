.next/
out/
build/
coverage/
playwright-report/
test-results/
node_modules/

# Documentation and upstream design reference — formatted by their own sources.
context/

# Generated by npm, not ours to format.
package-lock.json

# A mirror of context/Design/colors_and_type.css. Brand values change by
# re-copying from the kit, never by reformatting — Prettier lowercases the hex
# literals, which silently makes the mirror diverge from its source. _verify.mjs
# compares the token values; this keeps the text identical too.
src/app/globals.css
